import React, { FunctionComponent } from 'react';
import { CountriesList } from '../../components/CountriesList';

export const CountriesPage: FunctionComponent = (props) => {
    return <CountriesList />;
};
